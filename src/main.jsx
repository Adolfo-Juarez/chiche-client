import "./assets/stylesheets/index.css";
import React from "react";
import ReactDOM from "react-dom/client";
import Routered from "./Router";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Routered />
  </React.StrictMode>
);
